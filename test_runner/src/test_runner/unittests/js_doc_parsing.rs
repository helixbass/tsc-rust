#![cfg(test)]

// use speculoos::prelude::*;

mod type_expressions {
    // use super::*;

    // use harness::{Baseline, Utils};
    // use typescript_rust::parse_jsdoc_type_expression_for_tests;

    // fn parses_correctly(name: &str, content: impl Into<String>) {
    //     let content = content.into();
    //     let type_and_diagnostics = parse_jsdoc_type_expression_for_tests(content, None, None);
    //     asserting("no errors issued")
    //         .that(&type_and_diagnostics)
    //         .is_some()
    //         .map(|type_and_diagnostics| &type_and_diagnostics.diagnostics)
    //         .is_empty();

    //     Baseline::run_baseline(
    //         &format!("JSDocParsing/TypeExpressions.parsesCorrectly.{name}.json"),
    //         Utils::source_file_to_json()
    //     );
    // }
}
